const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),vm=require('node:vm');
const root=path.resolve(__dirname,'../recovered-frontend');
const parser=require(path.join(process.env.NDASH_PARSER_ROOT||root,'node_modules/@babel/parser'));
const source=fs.readFileSync(path.join(root,'src/pages/huddle-approvals/index.jsx'),'utf8');
function find(n,p){if(!n||typeof n!=='object')return;if(p(n))return n;for(const v of Object.values(n)){const r=find(v,p);if(r)return r;}}
const node=find(parser.parse(source,{sourceType:'module',plugins:['jsx']}),n=>n.type==='VariableDeclarator'&&n.id.name==='handleApprove').init;
const code='('+source.slice(node.start,node.end)+')';
function setup(status,{denied=false,missing=false}={}){
 const s={row:{id:'qa-huddle',status,approved_at:status==='approved'?'2026-09-15T12:00:00Z':null},writes:0,history:[],success:[],errors:[],loading:false,refreshes:0};
 const db={from(table){return {update(payload){const filters={};let single=false;const q={eq(k,v){filters[k]=v;return q;},select(){return q;},single(){single=true;return q;},then(resolve,reject){if(denied)return Promise.resolve({error:{code:'42501',message:'QA permission denied'}}).then(resolve,reject);const matches=!missing&&Object.entries(filters).every(([k,v])=>s.row[k]===v);if(!matches)return Promise.resolve(single?{data:null,error:{code:'PGRST116',message:'No matching QA Huddle'}}:{data:null,error:null}).then(resolve,reject);s.writes++;Object.assign(s.row,payload);return Promise.resolve({data:{...s.row},error:null}).then(resolve,reject);}};return q;},async insert(payload){assert.equal(table,'huddle_audit_log');s.history.push(payload);return {error:null};}};}};
 s.run=vm.runInNewContext(code,{reviewTarget:{huddle:{id:'qa-huddle',status:'submitted',offices:{name:'QA / Office A'}}},userProfile:{id:'qa-reviewer',full_name:'QA / Reviewer'},supabase:db,setActionLoading(v){s.loading=v;},setReviewTarget(v){s.closed=v===null;},success(...x){s.success.push(x);},toastError(...x){s.errors.push(x);},fetchHuddles(){s.refreshes++;}});return s;
}
for(const status of ['approved','rejected','unlocked','draft'])test('stale approval preserves newer '+status+' decision and history',async()=>{const s=setup(status),before=JSON.stringify(s.row);await s.run();assert.equal(JSON.stringify(s.row),before);assert.equal(s.writes,0);assert.equal(s.history.length,0);assert.equal(s.success.length,0);assert.equal(s.errors.length,1);assert.equal(s.loading,false);});
test('two reviewers produce only one saved approval and audit entry',async()=>{const s=setup('submitted');await Promise.all([s.run(),s.run()]);assert.equal(s.writes,1);assert.equal(s.history.length,1);assert.equal(s.success.length,1);assert.equal(s.errors.length,1);});
test('missing or invisible row cannot report approval',async()=>{const s=setup('submitted',{missing:true});await s.run();assert.equal(s.history.length,0);assert.equal(s.success.length,0);assert.equal(s.errors.length,1);});
test('ordinary authorized approval retains actor and successful UI behavior',async()=>{const s=setup('submitted');await s.run();assert.equal(s.row.status,'approved');assert.equal(s.row.approved_by,'qa-reviewer');assert.ok(s.row.approved_at);assert.equal(s.history.length,1);assert.equal(s.history[0].changed_by,'qa-reviewer');assert.equal(s.history[0].action_type,'approve');assert.equal(s.success.length,1);assert.equal(s.closed,true);assert.equal(s.refreshes,1);assert.equal(s.loading,false);});
test('permission denial has no history or success side effect',async()=>{const s=setup('submitted',{denied:true});await s.run();assert.equal(s.writes,0);assert.equal(s.history.length,0);assert.equal(s.success.length,0);assert.equal(s.errors.length,1);});
