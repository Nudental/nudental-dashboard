const fs=require('fs'),path=require('path'),assert=require('assert/strict'),parser=require('./phase4/rocket-source/node_modules/@babel/parser'),traverse=require('./phase4/rocket-source/node_modules/@babel/traverse').default;
const read=f=>fs.readFileSync(path.join(__dirname,f),'utf8'),meta=JSON.parse(read('phase4-ndash090-manifest.json')),r=JSON.parse(read('phase4-ndash090-runtime.json')),s=read('phase4/ndash090-assets/'+path.basename(meta.asset));let helper;
traverse(parser.parse(s,{sourceType:'module'}),{VariableDeclarator(p){if(p.node.id.name===r.rangeFunction){assert(!helper);helper=p.node.init}}});assert(helper);const before=s.slice(helper.start,helper.end),old='a.setDate((a==null?void 0:a.getDate())-e)',replacement='a.setUTCDate((a==null?void 0:a.getUTCDate())-(e-1))';assert.equal(before.split(old).length,2);const after=before.replace(old,replacement);parser.parseExpression(after);
for(const [variant,code] of [['before',before],['after',after]])fs.writeFileSync(path.join(__dirname,'phase4-ndash091-helper-'+variant+'.js'),code);
require('./phase4-version-graph.cjs')({issue:'091',previous:'090',deployment:'f6e39727-05b1-4546-bb4f-969a442989d3',target:'ndash090-GustoTimeAndAttendance.js',targetPatches:[],mainPatches:[{old:before,new:after}]});console.log(JSON.stringify({one_date_helper_changed:true,request_generation_component_unchanged:true}));
