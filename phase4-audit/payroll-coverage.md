# Payroll coverage — Phase 4 in progress

Dentrix Ascend read view: scheduled, custom, partial, empty, and future-year states checked. NDASH-021 prevents read-time mapping writes; NDASH-022 removes blank placeholder run; NDASH-023 prevents stale financial rows and misleading empty-state KPIs. Read-only fingerprint unchanged for 37 saved mappings after final live checks. Mapping tools, payroll/compensation editing, employee modifications, and email delivery intentionally untouched.

Imported from Gusto Overview: opens without browser errors; current and historical totals checked against private read-only API/database aggregates. Reconciliation RPC source contains only SELECT calculations; mount does not invoke the disabled sync RPC. Re-sync remains disabled. NDASH-024 period scopes/labels/monthly chronology and NDASH-025 off-cycle counts are deployed and live PASS. All Time157 runs/35off-cycle, 2025 33/7, current20/2.

Employees: NDASH-026 deployed/live PASS for selected detail/crosswalk, name/email search, state/type/benefit query scope, count and pagination after filtering. Total164 preserved; Active30 and full_time14 match original IDs. State and benefit columns are currently unpopulated, so only correct empty behavior is verified for those flags. NDASH-027 deployed/live PASS for page2 → Active returning to page1 and accurate empty-filter message. Employee writes/export file contents not exercised. Native keyboard activation worked reliably for offscreen pagination buttons; verify actual resulting state rather than assuming a click advanced it.

Payroll Runs: NDASH-028 backend deployed/live PASS: type/status/run-by filters and ascending ordering before pagination; Off-Cycle2, Regular18, Reversed0. Nearest-future ordering passes synthetic tests and historical API comparison; no future run currently exists. NDASH-029 deployed/live PASS for rapid filter/year changes, page2 filtering, summary scope and empty text; 89 tests and build PASS. Normal2026 all-runs20 restored.

Confirmed remaining Gusto defects, not yet repaired:
- Contractor spend KPI silently shows zero after `/v2/payroll/contractors` returns404. Contractor Spend by Year chart uses annual payroll net pay and even labels its series Payroll. Existing imported table has89 payments across2020–2025:64funded/noncancelled,23unfunded/noncancelled,2cancelled. No2026 rows. A repaired connection must distinguish actual paid/spend from unfunded/cancelled amounts, using the existing expense definition as evidence. Do not simply connect the endpoint and expose raw payment sums as paid spend.
- Contractor table requests50 rows per page but renders no pagination; Total Paid and unique contractor counts use only that page. Once connecting its source, provide accurate scope/labels and pagination so records cannot be silently omitted. Existing filtered-empty wording also says endpoint disconnected even when a valid filter could return0; 404 is swallowed as empty. These need scoped tests before release.
- Reconciliation badge says 100 unresolved offices, but source fetches up to 100 unresolved expense fact rows. Need count/label verification; it is neither a distinct-office count nor complete above the cap.
- Failed subqueries generally fall back to zero; never assume a rendered zero establishes data availability.

Not yet fully audited: remaining employee/run export, detail, filter/pagination combinations; Contractors, Benefits, Pay Schedules, Import History, Time & Attendance; Comparison and Provider Compensation subviews. Inspect mount effects before opening. Do not start imports or write workflows on real records. No Phase4 payroll fixtures created. Future stale-response tests for other Gusto hooks remain appropriate; do not assume their state handling is fixed by029.
